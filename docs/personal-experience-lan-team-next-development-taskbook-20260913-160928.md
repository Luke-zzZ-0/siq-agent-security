# SIQ Agent Security 后续开发任务书：个人闭环与局域网团队管理

> 执行进度更新：N01 已完成开发与 Linux 最低验收，见 [本批报告](evidence/personal-experience/n01-completion-20260913-190637/report.md)。Windows/macOS 原生项仍保留，其他任务状态见 [接续台账](personal-experience-closure-progress-20260913.md)。下方需求及最终目标不变。

> 文档版本：v2.0；编制时间：2026-09-13 16:09:28（Asia/Shanghai）。
> 仓库：`/home/maoyd/siq/siq-agent-security`；本次主线整合提交：`983b820ce85efe5902f33af6d4bb16f88c83ba82`，PR #32。
> 执行对象：GLM 开发窗口；实施方式：在现有项目增量开发、验证、留存证据。
> 目标顺序：先完成个人用户真实使用闭环与验收，再实施局域网团队多设备管控。
> 本文是新的执行入口；原任务书的 D01–D11、UX-000–015、LAN-001–006 保留。本文更新开发顺序、现状和验收要求，不取消任何已确认需求，不宣称产品整体完成。

## 1. 先读这一页

当前主线已经具备大量个人版基础：本地生命周期、管理会话恢复、发现与接入、权限管理、Skill 安装/更新/移除事务、任务活动、脱敏追溯包和可选加密原文仓。Linux 的 Hermes/OpenClaw 已有部分真实原生验证。接下来不能继续仅以“新增一个 API 和几个单测”为交付终点，应将这些能力串成用户能完成的完整旅程。

优先缺口有六项：通用状态格式兼容保护；生产 Git 获取的安全传输；自动检查 Skill 更新；可信 Skill 执行归属；真实平台审批后继续执行；三系统三平台的安装与运行验收。WorkBuddy 原生能力、Windows/macOS 默认通知和实际桌面投递也未完成。LAN 尚未形成多设备管理闭环。

**关键纠正：M131 的生产 Git 获取目前被审查修复显式关闭。** 本地 Git fixture 通过，不表示生产可以安全克隆；不得直接删除 `fetchGitCLI` 的拒绝逻辑或在 UI 宣称 Git 导入可用。M132 当前主要是手动新版检查及 UI，自动定期检查仍待实现。历史 M128 元数据匹配不能证明运行时 Skill 身份，仍须显示归属未知。

执行顺序：N00 基线 → N01 状态保护；同时可研究 N02 Git 和 N04 平台能力 → N03 自动更新、N05 可信绑定、N06 审批恢复 → N07 安装交付、N08 用户旅程 → N09 综合验收。N09 通过后才进入 T01–T06。某个实机阻塞时，继续没有该依赖的个人任务，不把阻塞改成已完成。

## 2. 产品目标、范围与安全底线

### 2.1 用户已经确认的方向

| 事项 | 必须实现的行为 |
| --- | --- |
| 系统与平台 | Windows、macOS、Linux；OpenClaw、Hermes、WorkBuddy 分别验证。CodeBuddy 适配器不等于 WorkBuddy 已支持 |
| 产品定位 | 发现和管理已有智能体/Skill、控制权限、安全安装、执行追溯；保留原平台日常任务界面 |
| 接入 | 优先原生钩子；必要时接受受控启动。先检查展示，用户确认权限后才启用保护 |
| 安装入口 | 沿用 Go 客户端 + 系统浏览器的生命周期方向，逐系统补交付包。Skill 作为轻量引导入口；命令行作为高级和自动化入口 |
| 权限确认 | SIQ 统一窗口；通知只导航。默认拒绝或允许本次，长期权限另行编辑 |
| Skill 来源 | 仓库/下载链接、本地目录；接入平台确实支持的前置安装拦截入口 |
| 更新 | 自动检查并展示内容与权限变化；用户确认具体候选后更新，不静默扩大权限 |
| 追溯 | 本机保存操作、授权、脱敏参数与结果证据；原文默认关闭、按需授权进入独立加密仓 |
| 团队 | 个人版独立可用，之后沿用同一客户端加入局域网团队，统一管理多台设备 |

安装包形态是 ADR-050 的实现方向，不意味着已经存在签名、可发布的三系统安装包。新增桌面框架、沙箱、第三方 Go 依赖或长期常驻组件，先通过 ADR 说明必要性和维护成本。

### 2.2 不能破坏的不变量

1. 复用 `apps/agentshield`、`apps/web/src/local`、现有适配器、Control API 和 Edge，不复制第二套决策引擎。
2. 先更新开发规格、版本化合同和示例，再改实现；`apps/agentshield` 保持标准库约束。
3. `declared`、`inferred`、`observed`、`effective` 分开；`effective` 只能来自后端读回，扫描或签名存在不能代替执行能力。
4. 必需 Authority 无效，在任何模式都硬拒绝；block 模式服务不可达拒绝。模型输出、Skill 自报身份、路径匹配不能生成授权。
5. 私钥只在受保护的本地状态目录；适配器、Skill、浏览器不持有签名私钥。管理 token 不进 URL、Web Storage、通知和日志。
6. 静态扫描不执行、导入或 eval Skill 内容；源码获取不运行仓库钩子、依赖安装脚本、LFS/submodule/filter。
7. 撤销、过期、主体/版本/参数变更后旧批准不可复用；追加式签名记录不能原地重写。
8. 企业 tenant_id 来自已验证身份；状态变化与审计/outbox 同事务；设备密钥服务端仅存哈希；生产不得使用 X-Dev-*、SQLite 或自动建表。
9. 当前本地模式是 `desktop-same-uid`。同一 OS 用户下任意恶意进程不在绝对隔离保证内；不能把 UI 确认或权限过滤包装为 OS 沙箱。
10. 不改写历史比赛/发布证据，不伪造实机、签名、发行、独立第三方或付费模型证据。现有合并授权不等于新版本发布或修改远端治理授权。

## 3. 代码基线与实际进度

### 3.1 主线整合说明

PR #32 纳入本轮核查时的全部本地/origin 分支头，以及开放 PR #25/#26/#29/#30/#31 的开发历史；M132 审查修复保留。历史 Pages 分支整棵树归档到 `docs/archive/gh-pages-9a4ebdc/`，不覆盖当前 `site/`。各分支未删除。

整合记录与 captured head 清单见 [main-integration-20260913.md](evidence/personal-experience/main-integration-20260913.md)。后续自动生成的 Pages 提交属于部署产物；不要为追逐每次自动部署反复合并生成分支到源码根目录。

GLM 原工作目录可能仍显示大量未提交文件：大部分内容已经经独立提交进入主线。与 M132 提交不同的四份历史记录列在该整合记录的 `uncommitted-remainder.json`。这些文件保留原状；必须基于新主线另建工作树、逐项比对，禁止整批再次提交旧目录。

### 3.2 当前状态表：实现和验收必须分开

| 原任务 | 当前已有实现/证据 | 不能据此声称完成的部分 | 后续任务 |
| --- | --- | --- | --- |
| UX-000 规格与基线 | 原需求、ADR、M1–M132 台账及独立修复记录 | 台账批次数不是产品完成比例 | N00 |
| UX-001/002 平台与信任设计 | 18 个 OS/CPU/平台候选的材料合同、校验器；ADR-049/050 | 候选矩阵不代表 18 组合已支持；缺真实 OS/平台材料 | N04/N07/N09 |
| UX-003 生命周期 | Go 初始化、启动/停止、服务配置、升级/恢复等；Linux 子集实测 | 完整安装包、登录/重启/卸载旅程；macOS/Windows 实机 | N01/N07 |
| UX-004 管理恢复 | 恢复会话和诊断实现，Linux 浏览器验证 | 三系统安装后完整旅程仍须复验 | N08/N09 |
| UX-005/006 发现与接入 | 实例、Skill 发现、管理接入、自检；Hermes/OpenClaw 部分原生材料 | WorkBuddy 原生接入、跨系统和自定义 profile 的完整闭环 | N04/N08 |
| UX-007 权限 | 编辑/部署/撤销、场景限制；M129 修复实际效果限制和场景冲突 | Skill 可信执行来源、多 Skill 真实边界、跨平台执行验证 | N05 |
| UX-008 确认 | 统一 inbox、通知调度、失败退避与敏感输出丢弃 | 默认 macOS/Windows 通知、实际投递、原生审批恢复、任务内授权 | N06/N07 |
| UX-009 来源/安装 | 本地目录、HTTPS ZIP；暂存/检查/目标绑定/安装事务及页面 | 生产 Git 关闭；原生安装拦截、平台完整识别待补 | N02/N04/N08 |
| UX-010 更新/移除 | 内容/权限比较、确认更新/恢复、移除；M132 手动检查 + UI | 自动检查、通用状态兼容防写、真实平台升级/回退验收 | N01/N03/N09 |
| UX-011/012 活动/UI | 聚合、详情、结果核验、来源追溯、签名脱敏下载 | 完整真实任务证据链、首页状态一致性与跨设备视图未闭环 | N08/N09 |
| UX-013 隐私 | 独立加密原文仓、逐任务授权/撤销、TTL 清理、默认导出隔离；Linux 子集 | 三系统真实采集、故障清理、端到端隐私验证 | N08/N09 |
| UX-014 发行 | 制品/兼容声明、摘要绑定、切换/恢复基础 | 通用状态迁移、正式签名制品、三系统升级与卸载验收 | N01/N07/N09 |
| UX-015 综合验收 | 分项测试和 Linux 材料 | 尚未完成个人版整体验收 | N09 |
| LAN-001–006 | 既有企业 Control API/Worker/Edge 可复用 | 尚无本轮局域网多设备完整交付 | T01–T06 |

本次整合已验证 Go 37 包、Web 73 项及构建、研究 unittest 66 项、Python 合同与研究测试、站点与工作流检查。整合另修复密钥扫描历史范围、研究 Ruff 和 Hermes MCP 组件脚本的前置授权流程。PR #32 最终 head `cbf44be` 的远端检查为 39 项通过、3 项按条件跳过，已于 2026-09-13 合并 main。这些数字是基线回归，不是跨系统产品验收分母。

### 3.3 必须优先参考的文档和代码

- 原范围：[原始任务书](personal-experience-lan-team-development-taskbook-20260910-145507.md)；历史：[开发台账](personal-experience-development-progress-20260910.md)。
- 本地事实源：[开发规格](agentshield-dev-spec-v1.md)、[设计](agentshield-design-v1.md)、`packages/contracts/`、`docs/adr/`；先读适用 `AGENTS.md`。
- 实机证据标准：[平台验证规格](personal-platform-validation-spec-v1.md)、[ADR-049](adr/0049-personal-platform-evidence-inventory.md)、[ADR-050](adr/0050-personal-client-lifecycle-direction.md)。
- 必须保留的修复：[M124–128](evidence/personal-experience/stage-review-fixes-20260913.md)、[M129/130](evidence/personal-experience/stage-fixes-m129-m130-20260913.md)、[M132](evidence/personal-experience/stage-fixes-m132-20260913.md)。
- 本地模块：`internal/state`、`clientrelease`、`inventory`、`adapterinstall`、`runtimeidentity`、`grant`、`pending`、`notify`、`skillimport`、`skillinstall`、`receipt`、`rawcontent`，均位于 `apps/agentshield/`。
- 平台桥：`adapters/runtime/hermes-agentshield/`、`adapters/runtime/openclaw-agentshield/`；`connectors/workbuddy/` 是发现入口，运行时能力另验。
- 企业入口：`apps/control-api/app/routers/environments.py`、`models.py`、`worker.py`、迁移目录；设备协议与执行：`edge/agent/`。

## 4. 实施规范与任务依赖

每个任务依次交付：差距和规格 → 合同/示例及兼容策略 → 后端与 CLI → 必要 UI → 正负向与故障恢复测试 → 实机或明确阻塞材料 → 当前状态记录。已有实现应扩展或修复，不因任务重新编号重复造轮子。

| 顺序 | 工作包 | 依赖与退出条件 |
| --- | --- | --- |
| A | N00 基线；N01 状态兼容保护 | N00 完成后实施；防止后续升级继续扩散不兼容状态 |
| B | N02 Git 安全获取；N04 平台能力核实 | 可分别推进，不等待所有实机；Git 未通过保持关闭 |
| C | N03 自动检查；N05 Skill 可信归属；N06 审批恢复 | N03 依赖 N01，Git 子项依赖 N02；N05/N06 根据 N04 的真实能力选方案 |
| D | N07 安装/通知/发行；N08 完整个人旅程 | 安装研究可提前，最终验收依赖前述安全能力 |
| E | N09 个人验收 | 所有个人要求有明确通过证据；未支持组合不能隐去 |
| F | T01–T06 团队阶段 | N09 通过后实施；前期只可整理接口差距，不挤占个人阻塞修复 |

任务状态只能为 `todo / doing / blocked / verified / accepted`。`verified` 表示本任务定义的验证已过；`accepted` 表示独立复核且用户旅程验收满足。外部资源缺失使用 `blocked` 并记录继续条件。完成一个后端子任务不得把父任务整行改成完成。

## 5. 个人阶段详细任务

### N00：从合并主线建立干净、可追溯的开发基线

**范围：** 对应 UX-000；首先执行。

1. `git fetch origin --prune`，记录 `origin/main` SHA 和当前各工作树状态；确认包含 PR #32。不得在仍运行的 GLM 旧目录切换分支。
2. 从 `origin/main` 创建新的 `glm/personal-closure-<timestamp>` 分支及独立工作树。分支名可按仓库约定调整，目的为隔离旧工作区。
3. 对旧目录剩余内容逐文件比较。已有源码不重复提交；确有未纳入修复时，说明行为差异、复现和依赖后单独转移。
4. 新建本轮进度文件，列出 N00–N09、T01–T06、原 UX/LAN 映射、状态、证据、阻塞和下一步。更新 `personal-experience-current.md` 顶部当前入口，历史记录不改写。
5. 运行必要基线检查，登记环境版本和未能运行的检查。若主线或所用宿主已经前进，重新核对差距，不套用本任务书的旧数字。

**验收：** 新目录干净、主线包含性明确；旧目录/索引未丢失；进度文件不宣称整体完成；任何未通过的基线检查都有真实失败原因和修复任务。

### N01：统一状态格式兼容、迁移与回退保护

**范围：** UX-003/010/014。主要位置：`internal/state`、`internal/clientrelease`、`cmd/agentshield`，以及各自管理持久化数据的本地模块。

**问题：** 发行制品兼容声明与 Grant 的 revision 并不等于整个状态目录的 reader/writer 兼容协议。`versions_test.go` 的历史修订测试不能代替状态格式保护。

**实施：**

1. 枚举所有持久化格式和写入入口：serve、初始化、CLI 管理、升级/回退、授权、准入、安装、原文和后台清理。设计统一兼容元数据、受支持 reader/writer 范围、状态实例绑定和单写者锁顺序；规范必须区分程序版本与格式版本。
2. 新程序访问已有状态时先读取并校验兼容性，再生成默认文件、私钥、配置或启动后台写任务。不兼容状态返回稳定错误，UI 显示可执行恢复方案；只读能力也必须通过 reader 校验。
3. 缺失 marker 的历史合法目录只能通过有边界的旧格式识别/显式迁移接纳。损坏、截断、未知字段、未来格式、符号链接不得被当作新目录自动初始化；空目录初始化是独立路径。
4. 设计受锁保护的迁移计划、备份、日志和提交点；失败/断电可以重新恢复。不得原地重签历史回执，不得恢复已撤销或已过期的权限。旧格式读写与迁移路径要明确支持窗口。
5. 将升级预检、切换、健康读回和回退与该协议绑定。兼容性不满足时不能先停止可用服务、替换程序再报错。
6. 明确威胁边界：不能事后强迫任意历史二进制或同 UID 恶意进程尊重新 marker。对纳入支持的旧版本落实检查或通过受控启动链拒绝不兼容程序；无法约束的旧程序如实说明，不声称绝对阻止。

**正负向验收：** 空目录初始化、合法旧格式迁移、新格式重启；未来格式/损坏 marker 全写入口拒绝；拒绝前后文件内容/模式/清单一致；并发初始化/迁移单写者；迁移每个故障点重启；新版本写状态后受支持旧版本拒写；不兼容回退不复活旧权限。至少真实 Linux 二进制演练，另外两系统保留原生验收项。

**完成门槛：** 提供写入口覆盖清单及测试映射，不能只在 `serve` 顶层增加一次判断。无实际旧二进制演练只能记组件 verified。

### N02：恢复有真实安全边界的仓库来源获取

**范围：** UX-009/010；主要位置 `internal/skillimport/{git,upstream}.go`、下载实现、`internal/skillinstall`、服务端和前端来源入口。

**实施：**

1. 先形成 ADR：是支持有限公共托管商的 HTTPS API → 固定 commit 归档，还是实现通用 Git HTTPS 传输。前者应明确供应商/URL 支持范围；不能把归档下载伪装成任意 Git clone。私有源认证单独设计安全引用，缺失能力明确不可用。
2. 出站策略必须作用于实际连接：校验并绑定 DNS/IP，拒绝私网/loopback/link-local/IPv4-mapped 等被禁地址及混合解析；重定向重新校验，防 DNS 重绑定。只检查原 URL 不够。
3. 通用 Git 方案还须覆盖协议内部的跳转、alternate/object URL 和子进程网络连接。清除危险代理/配置/凭据 helper 环境；禁用其他协议、钩子、submodule、LFS/filter 和任意代码执行。不能用可注入的用户 `.gitconfig` 或浏览器 Cookie。
4. 解析引用后固定不可变 commit；SubDir 和工件摘要绑定。获取、解包/checkout、文件数、单文件/总大小、磁盘和时间均有上限；取消/超时/失败清理临时目录。
5. 补完整 Git create/result/list、来源元数据与更新比较合同，核对现有 v2 消费端兼容。来源 URL 中的凭据和查询敏感值不进入持久化审计、日志、API 错误或追溯包。
6. 只有生产真实调用路径通过验收后才启用 Git；继续复用同一静态检查、权限预览、确认和安装事务。

**必须测试：** 正常公共仓库固定 commit/SubDir、分支前进被检查识别；跨 host 重定向、解析混合地址、重绑定、协议走私、恶意 Git 配置、超大对象/归档、路径逃逸、超时、取消、远端删除；测试缝和生产传输分开登记。真实托管服务至少一个成功和一个受控失败，报告不含私有仓库身份或凭据。

**完成门槛：** 审阅者能从 URL 到最终连接/内容验证完整边界；只有本地 `file://` fixture 不算完成。没有安全方案时保留拒绝并记录具体阻塞，不降级为不受限 clone。

### N03：自动新版检查、变化提示与用户确认更新闭环

**范围：** UX-010；复用 `update_check.go`、`update_compare.go`、现有更新事务、`SkillUpdateCheckPanel` 和已安装 Skill 页面。

**实施：**

1. 当前 ZIP 记录以来源定位摘要绑定，调用方再次提供 URL；浏览器仅在内存保存 URL。自动检查不能偷偷将完整地址塞进 Web Storage 或审计。先设计可自动重取的来源配置：安全公共定位信息与私有/临时认证引用分离，保存范围和用户控制明确；不满足条件的安装显示“需要重新提供来源”。
2. daemon 提供有界调度：建议默认每 24 小时、随机抖动、有限并发、失败退避；这些是实现默认值，写入规格并测试，可由用户关闭。手动检查优先但不产生并发重复任务，重启后避免集中请求。
3. 检查只更新独立检查状态/调度元数据，不修改安装版本、Grant、准入工件或更新操作。保持现有 `CheckUpdate` 对安装状态的只读保证；测试明确允许哪些调度文件写入。
4. 状态包括未检查、检查中、已最新、有新版、来源不可用、暂不支持；显示最后成功时间，失败不能显示“已最新”。截断差异显示实际数量/截断提示；`deferred_to_update_comparison` 不能显示成权限无变化。
5. “有新版”只启动已有比较/确认流程；再次取得候选后绑定不可变内容和权限差异。来源再次前进、候选过期、安装/移除并发、Grant 修订变更时旧检查结果不能授权更新。
6. 真实宿主更新后验证发现、平台识别、允许操作、越权拒绝、旧会话/旧授权失效及恢复。更新与移除保持显式确认。

**必须测试：** 受控时钟的调度/重启/退避、关闭后不发新请求、离线后恢复、取消与迟到响应、私有/过期来源、来源替换拒绝、210 项差异截断、零安装状态写入、用户确认前版本不变、TOCTOU 旧候选拒绝。通过真实 HTTPS ZIP/受支持 Git 与一个原生平台的更新旅程，再补全部要求的组合。

### N04：核实三平台真实能力并补接入适配

**范围：** UX-001/002/005/006/009；复用平台材料合同和 `scripts/personal-experience/platform_acceptance.py`。

1. 建立 Windows/macOS/Linux × OpenClaw/Hermes/WorkBuddy 的九格产品矩阵，并列出现有 18 个 CPU 候选的对应行。逐项记录宿主实际版本、获取方式、发现、配置备份、pre/post 钩子、调用 ID、Skill 来源、审批等待/重试、安装拦截、保护移除能力。
2. 对 WorkBuddy 单独查官方文档和实机配置/行为；记录来源和日期。发现 Connector、CodeBuddy settings 或 SDK 示例都不能替代 WorkBuddy 执行链证据。
3. 每项标明 `native / controlled_start / unavailable / unverified` 及理由。厂商不提供某系统发行版时，保留需求缺口并给出可验证替代方案或提交用户范围决策，不能自行减少首发范围。
4. 扩展实例发现与配置备份/恢复：多 profile、非默认路径、空格/中文路径、多个实例、实例移动/移除、配置变化后的陈旧计划拒绝。每次操作绑定准确实例，不能修改另一套配置。
5. 接入后只显示经过当前实例自检的能力；通过安装和发现不能直接显示“全部受保护”。后端不可达、身份失效、钩子未加载、平台升级不兼容分开诊断。
6. 原平台安装入口只有在真实安装前能拦截时才接管；无钩子时展示事后发现或受控安装路径，不注入平台不支持的字段。保持 OpenClaw `security.installPolicy` 回归修复。

**验收：** 每个平台至少一条“发现 → 预览 → 接入 → 正常允许 → 越权阻断 → 服务离线阻断 → 配置还原”原生旅程；缺某 OS 的对应行保持 blocked。采集实际命令/版本/脱敏证据和清理结果，不安装到用户正在工作的默认实例。

### N05：可信 Skill 执行归属与真实权限交集

**范围：** UX-007/011；主要位置 `runtimeidentity`、`grant`、`receipt`、Skill 安装记录和平台桥。

1. 从 N04 选择可证明的宿主调用元数据或受控启动链。运行归属应绑定平台实例、agent、session/task、Skill 的安装记录与不可变内容摘要、调用 ID 和有效期，信任根来自本机受信宿主链。
2. `SKILL.md` 文本、自报 skill_id、工具参数、cwd、文件名或内容匹配只能提供线索，不是可信执行证明。没有证据继续 unknown，不凭“宿主通常这样做”启用强制归属。
3. 设计单次调用中多个 Skill、嵌套/委派及会话切换的权限边界。默认禁止任何不明确的扩大；算法在规格中定义 Agent、Skill、场景及已有必需 Authority 的交集，避免新建一套平行 policy。
4. 所有决策和结果关联绑定同一调用；重复调用 ID、先 post 后 pre、跨 session、重放、版本切换、撤销和过期均不能扩大授权。保留 Hermes/OpenClaw 一次性关联修复。
5. 强制归属开关只有该组合的真实证据和兼容策略通过才允许启用。UI 区分“实例保护已启用”和“Skill 级归属尚不可验证”。

**必须测试：** 伪造/缺失/篡改 claim、相同内容不同安装、同名 Skill、跨实例、A Skill 复用 B 授权、旧版本和旧会话、混合调用、撤销并发、场景不一致 409、不支持效果拒绝。至少真实宿主越权请求被拦截且没有实际副作用。

**完成门槛：** 必须有运行时可信来源证据和真实执行层拒绝；仅元数据核验和 API 签名测试不能关闭 UX-007。

### N06：统一确认后的真实继续执行与通知

**范围：** UX-008；复用 pending/inbox、现有签名批准机制、平台钩子和 `internal/notify`。

1. 先复现 Hermes 回调等待上限与 OpenClaw/WorkBuddy 的恢复语义。选择平台支持的暂停恢复或受控重试；不能仅延长 HTTP timeout、阻塞无限等待或让模型“记住已批准”。
2. 同一请求生成有期限、可审阅、可撤销的待办。批准绑定主体、具体操作/资源、关键参数摘要、Skill 版本、Grant/组织策略修订；在最后可用执行检查点重新验证。
3. 一次批准只消费一次；客户端重试、daemon 重启、响应丢失和重复通知不得重复执行。平台不能透明恢复时，明确显示“已批准，请返回原平台重试”，并保证重试安全；该替代是否满足完整体验须单列验收。
4. 默认“拒绝/允许本次”；任务内同类授权单独定义次数、资源范围、任务寿命和撤销。硬禁止和无效必需 Authority 不进入可批准绕过路径。
5. 补齐 Windows/macOS 的默认通知方式与真实投递。通知内容只含必要计数/概要，点击进入正确 SIQ 待办；通知不能携带管理 token、原始命令或直接批准。
6. 通知权限被拒、无图形会话、命令失败时降级至 inbox，保持独立退避；不记录通知子进程原始输出。系统通知是否自动导航须实机证明。

**验收：** 用户未回应不执行；允许一次只产生一次真实结果；拒绝/过期/改参/换 Skill/撤销后均不执行；重启/重复回调/网络断开不会重复副作用。每系统至少实际通知与恢复证据，headless 测试不能代替桌面投递。

### N07：三系统安装包、后台生命周期和升级交付

**范围：** UX-003/014，依赖 N01；通知部分依赖 N06。

1. 复用 ADR-050 和现有 Go CLI/systemd/LaunchAgent/Task Scheduler 实现，核对现有命令再扩展；普通用户安装后不要求自行安装 Python、npm 或构建工具。
2. 设计稳定程序路径、Go embed 前端、用户级服务注册、首次配对/打开页面、重复安装和版本检测。明确安装目录与状态目录，默认不提权、不侵入系统服务。
3. 每 OS 验证登录自启、退出/重启、故障恢复、端口占用、服务身份与进程/配置所有权。拒绝外来配置和未知进程；清理只删除本产品拥有的对象。
4. 结合现有 clientrelease 的签名清单、兼容声明、二进制摘要、快照、切换日志与 N01 状态保护，完成跨不同版本的升级/失败恢复/回退。测试开发密钥不能作为正式发行证明。
5. 卸载默认保留用户数据并可明确选择清理；提供卸载预览和恢复说明，不能删除共享 Skill、别的实例配置或其他产品服务。
6. 制品命名记录版本、OS、CPU、来源 SHA、构建工具链、摘要、SBOM/来源材料和签名状态。交叉编译属于构建证据，代码签名/notarization/SmartScreen 等真实发行状态分别登记；缺外部凭据时准备材料并标阻塞。

**验收矩阵：** 三系统干净用户安装 → 打开管理 → 登录/重启可用 → 升级 → 故障恢复 → 卸载；空格/中文路径、非管理员、重复安装、被占端口、丢失旧程序、篡改新旧制品、未来状态、权限不足均有负向。每项保留 OS/CPU/版本/实机证据，不能以 mock 返回码完成整行。

### N08：打通首页、安装、权限与追溯的个人用户旅程

**范围：** UX-004/005/009/011/012/013；主要位置 `apps/web/src/local/` 与其已有 API。

1. 首页回答：服务是否可用、哪些实例已发现、哪些真正启用保护、哪些待确认、最近发生什么、有何需要处理。状态来自同一后端事实，禁止不同页面各自推测保护状态。
2. 首次引导使用“安装客户端 → 发现 → 检查 → 确认权限 → 接入与自检 → 开始使用”。每步有可恢复错误；发现时不自动改用户配置。
3. 连接诊断区别未启动、认证过期、错误端口/入口、版本不兼容、实例失效；提供可执行动作。`dev:local` 的 `/`、`/demo` 深链继续进入本地入口，正式 Go embed 单独验。
4. Skill 导入支持安全的本机目录选择/导入方案和链接，解释浏览器能力边界；不绕过暂存、安全检查或确认。不可用 Git 和不可拦截原生入口准确显示能力限制。
5. 将一次真实任务的主体、Skill 版本/unknown、授权、决策、观察结果、效果证据和撤销关联起来。区分允许、执行中、已观察、结果已核验、失败/结果未知，不能“有回执 = 任务成功”。
6. 脱敏追溯包默认不包含原文；原文仅在独立授权和二次查看流程中读取。补磁盘满、TTL、清理重启、并发撤销、损坏密文、导出泄漏负向；敏感 URL/凭据不出现在错误/浏览器存储中。
7. 做键盘操作、焦点、中文长文案、小屏、加载/空/失败/重试、取消与陈旧响应验证；复用现有组件，不更换整套视觉系统。

**验收：** 使用真实后端完成首次接入、允许/拒绝、安装与更新、活动追溯、原文启停、会话刷新恢复。浏览器 fixture 单独标记，不替代真实宿主；“首次成功所需时间/步骤”实测记录后优化，不能宣称未经测量的五分钟完成。

### N09：个人版综合验收与团队阶段准入

**范围：** UX-015，关闭个人阶段的唯一总体门槛。

1. 为九个 OS/平台产品组合及对应 CPU 候选维护可复查矩阵。每行至少记录安装、发现、接入、权限允许/拒绝、额外审批、Skill 安装/更新/移除、归属与追溯、服务失联、重启恢复、卸载、隐私；分清哪些为 OS 公共项，哪些必须逐宿主验证。
2. 每条证据带代码 SHA、二进制摘要、OS/CPU、平台版本、开始/结束时间、命令、退出状态、预期/实际、脱敏日志及清理说明。分类使用静态检查、单测、mock、组件集成、原生实机、完整验收，不混算。
3. 先在可用 Linux 环境形成完整基准旅程；Windows/macOS 和 WorkBuddy 用真实环境执行同一验收定义。安装/服务/授权均在隔离用户或测试 profile，避免影响用户现有任务。
4. 所有真实权限绕过、未授权写入、凭据泄漏、错误恢复导致数据损坏等高优先级问题修复并复验后才能通过。
5. 独立审阅 N01/N02/N05/N06 的安全负向与 UI 真实状态。缺宿主支持时给出可实现替代与代价，由用户明确决策后才变更目标，不通过改表静默缩减范围。
6. 输出个人版验收报告、可安装候选清单、已知限制和团队交接。没有三系统三平台所需证据则保持个人阶段未完成，继续推进不依赖阻塞资源的个人工作。

**通过定义：** 原 UX 各项验收条件有可复查证据，用户要求的组合或明确批准的新范围全部通过；正式发行的外部动作单列，不能通过组件测试自动发布。

## 6. 团队阶段详细任务（N09 通过后执行）

### T01 / LAN-001：复用控制面的局域网部署

- 核对现有 Control API、Worker、PostgreSQL、企业 UI 与迁移，不新建独立团队后端。输出实际拓扑、DNS/TLS、OIDC、首次管理员、配置/Secret 引用、健康、备份与恢复说明。
- 开发 Compose 的 loopback/开发身份不能原样作为团队生产部署。生产走 TLS 和已验证身份，干净 PostgreSQL 执行 Alembic；不能自动建表或依赖兄弟项目数据库。
- 设备主动出站连接控制面；个人管理 API 默认仍仅 loopback，不为方便团队访问直接暴露本机管理员端口。
- 验证远端局域网设备的真实访问、身份拒绝、证书错误、迁移与备份恢复；全部关键写操作断言审计/outbox 同事务。

**完成门槛：** 一台团队控制面与至少另一台真实设备经受控入口通信；单机容器互通只是组件证据。

### T02 / LAN-002：加入团队、设备身份和退出

- 复用 `environments.py` 的 enrollment/register/heartbeat 和 Edge 身份。个人 UI 解释团队归属、上报范围和策略影响，经明确确认后加入。
- 单次、限时接入码并发消费安全；身份来自注册结果和验证凭据，服务端仅存设备凭据哈希。设备端用系统安全存储/受保护引用，不能前端持久化明文。
- 完成拒绝加入、心跳、离线、吊销、退出、重新加入、凭据轮换、设备重命名和重装。团队身份不能覆盖本机个人状态实例 ID。
- 在线每次请求校验吊销；离线设备无法即时收到撤销，应明确最后可信状态、过期和重连行为，不宣称离线立即生效。
- 退出前显示组织要求与可保留个人数据；本机个人模式恢复不能复活已撤销的组织授权。

**负向验收：** 重放/过期码、并发注册码、旧凭据、跨团队身份、冒名设备、重装冲突、断网中途加入与退出；两台真实设备材料分开。

### T03 / LAN-003：多设备资产、归属与隐私

- 资产键至少由 tenant、device、platform instance、agent/Skill 安装身份构成；同名和相同内容跨设备不能错误去重。
- 复用 inventory 上报与事件合同，增加必要版本字段；区分当前快照、删除/失联、最后观察时间和来源。用户选择上报范围，默认不上报原文、秘密、完整私有路径/URL。
- 企业 UI 展示设备 → 实例 → 智能体 → Skill 关系、真实保护能力和状态；不能把 last_seen 当在线、安装当受控。
- 设计数据保留、脱敏和租户/角色可见性；增量/重放幂等，重连不复活已移除资产。

**验收：** 两设备同名资产互不覆盖；跨租户定位与授权按仓库规则拒绝；断网/删除/重装/乱序事件状态一致，默认上报包无原文或凭据。

### T04 / LAN-004：定向与批量任务

- 当前环境级竞争领取不能充当广播。扩展明确 `target_device_id`，批量请求展开独立子任务，并绑定固定目标快照、父批次和各设备结果。
- 复用现有 lease/claim/ack/outbox；领取原子性、幂等键、重试、租约到期、取消、部分成功有协议定义。一个设备不能领取另一个设备的任务。
- 不把网络投递 exactly-once 等同真实副作用恰好一次；不可幂等动作需要执行日志和去重/人工恢复策略。任务回复只有有效设备身份与目标绑定才能改变状态。
- 企业 UI 可见排队/已领取/运行/成功/失败/超时/取消及每设备明细；部分完成不能显示全批成功。

**验收：** 两设备目标产生两份独立结果；同时抢租约、掉线重领、重复回执、跨设备/租户请求、过期签名、部分失败和取消竞态被正确处理。

### T05 / LAN-005：组织策略的实际应用与回报

- 策略含发布者身份、tenant/device 或组范围、版本/摘要、生效/到期、签名及撤销语义；本机验证后经现有权限引擎应用，不能绕过本地安全检查。
- 个人授权不能放宽组织上限；定义冲突的保守交集和解释 UI。组织不可接受的操作不能弹出个人“允许”绕过。
- 明确 `published / received / applied / verified`：收到字节不等于生效，生效须后端配置读回，verified 还需要真实允许/拒绝验证。
- 断网使用有期限的最后可信策略；过期和需在线批准的行为按定义拒绝。恢复联网验证撤销/版本单调性，不能接受旧策略重放。
- 应用失败可恢复；权限状态、审计和回执关联同一修订。没有后端执行能力的限制标不可用，不能仅在企业页面显示绿色。

**验收：** 篡改、旧版本、错租户/目标、未知签名、过期、离线、冲突、只收到未应用、应用中崩溃；两台真实设备各自证明实际行为改变且越权未产生副作用。

### T06 / LAN-006：两台真实设备闭环及扩展

- 使用至少两台真实局域网设备，优先不同 OS，逐台完成加入 → 资产 → 定向/批量任务 → 策略 → 允许/拒绝 → 离线 → 重连 → 吊销/退出。
- 核验本机与企业回执、时间、设备身份、任务和策略版本关联；默认隐私边界不随加入团队消失。
- 增加多设备模拟负载、数据库竞争、重试风暴、时钟偏差、网络抖动、积压清理测试，记录实测容量和瓶颈。模拟规模不能替代两实机验收，也不能凭单次测试承诺企业容量。
- 交付部署/升级/备份/故障排查/退出手册，记录已知限制和支持矩阵；独立审阅租户隔离和权限生效证据后关闭团队阶段。

## 7. 检查命令、证据与完成度口径

### 7.1 按修改范围执行，不凭记忆勾选

以下为仓库已有检查入口；工作目录和依赖先核实，不需要每个文档改动都重复运行所有产品测试。安全与合同改动必须按 `AGENTS.md` 的完整要求执行，最终合入看最终 HEAD 的 CI。

```bash
# 仓库根目录
 git diff --check
 python3 scripts/check_ci_action_pins.py
 python3 scripts/check_site.py
 python3 -m unittest discover -s scripts/research -p 'test_*.py'

# apps/agentshield
 gofmt -l .
 go vet ./...
 go test ./...
 # 针对并发/状态/授权变更运行受影响包的 go test -race；交叉构建按既有矩阵。

# apps/web
 npm ci
 npm run test
 npm run build
 npm run build:local

# apps/control-api
 uv sync --dev --locked
 uv run ruff check app
 uv run pytest

# 根目录，使用已准备的 Python 环境
 apps/control-api/.venv/bin/pytest -q apps/control-api/app/tests/test_schema_contracts.py
 apps/control-api/.venv/bin/pytest -q adapters/runtime/hermes-agentshield/tests
 apps/control-api/.venv/bin/ruff check scripts/research scripts/check_research_task_ledger.py
 python3 scripts/validate-mcp-provenance.py --hermes-bridge --out /tmp/siq-mcp-bridge-report.json
```

OpenClaw 适配器、个人浏览器、原生安装/升级脚本按各自 README 和当前 CLI 参数运行，先 `--help`/读源码确认，不臆造选项。真实托管服务联测使用测试仓库；不得用仓库里的测试注入缝冒充生产路径。密钥扫描继续使用 CI 校准的固定版本和完整可达历史，不能为过门禁忽略错误或扩大例外。

### 7.2 证据结构

每批目录建议 `docs/evidence/personal-experience/<task-id>-<YYYYMMDD-HHMMSS>/`；团队使用独立团队证据目录。至少包含：

- `verification.json`：任务 ID、原 UX/LAN 映射、基线/实现 SHA、OS/CPU、平台/工具版本、二进制 SHA256、证据类型、命令及退出码、通过/失败/未运行项。
- `report.md`：问题与最终行为、测试覆盖、原生观察、限制、恢复/清理结果；截图只作辅助，不能替代后端证据。
- 脱敏日志或签名材料及摘要。报告不得保留 token、私钥、带凭据 URL 或用户原文。失败材料也保留，以新批次复验关联，不能覆盖旧结果。

新增证据结构若用于机器读取，先定义版本合同及正负向示例。测试分母保持稳定，新增测试带来数量变化时记录原因；不把同一套 fixture 换几个名字当更多平台覆盖。

### 7.3 进度怎么报告

分别报告：原 UX 任务验收关闭数/16、LAN 关闭数/6；当前 N/T 工作包通过情况；九个产品组合与 CPU 候选的实机覆盖；剩余阻塞。子任务工程覆盖可单独说明，但不能将 M132/批次数量解释成总体百分比。

若提供预估，必须说明估算分母、权重和外部依赖；缺实机、平台发行版或签名凭据时不能承诺准确完工时间。当前结论是“个人功能基础较多，完整个人验收与团队闭环尚未完成”。

## 8. 给 GLM 的执行约定和首批交付

### 8.1 第一批只交付 N00 + N01 的可审查增量

1. 记录最新主线与独立工作树，确认没有遗漏已提交修复。
2. 先提交到工作区的状态写入口清单、兼容协议规格/合同草案，明确受支持旧格式和同 UID 边界；常规实现选择自主完成。
3. 落实不兼容状态拒写和关键负向；补真实 Linux 升级/回退演练；缺外部环境列阻塞。
4. 同时只做 N02/N04 的读取调研与可复查能力表，不提前打开不安全 Git 或放松 Skill 归属。
5. 更新本轮进度与证据，形成独立可审查提交范围。无新授权时，沿用仓库关于提交/推送/发布的要求；用户已在当前会话明确授权的动作不重复询问。

下一批从依赖满足的 N02/N03/N04 开始。每批必须有用户行为或安全性质的完整验证，避免再次形成大量尚未闭环的小 API 批次。

### 8.2 可直接复制给执行窗口的指令

> 以本任务书 v2.0 为后续开发执行入口，保留原 D01–D11 和 UX/LAN 范围。从最新 origin/main 新建独立工作树，先完成 N00 与 N01，再按依赖推进。复用现有项目，规格和合同先行，安全修复附负向测试。保留生产 Git 拒绝、必需 Authority 硬拒绝、Skill 归属 unknown、一次性回执关联、通知退避和隐私修复，只有完整替代方案验收通过才能改变相关状态。不要改正在使用的旧 GLM 工作区，不批量提交旧改动。实机缺失记录 blocked 并继续无依赖个人任务；组件、mock、交叉编译不等于原生验收。每批落盘实现、测试和脱敏证据，报告准确状态。先个人综合验收再团队，不静默减掉 WorkBuddy 或任何目标系统，不发布制品或修改仓库治理来替代开发验收。

### 8.3 每批报告模板

```text
任务：Nxx / 对应 UX-xxx；状态：doing / blocked / verified / accepted
基线 SHA / 当前 SHA / 工作树：
具体问题与用户可见的最终行为：
规格、合同、实现、UI、测试和证据文件：
实际运行的命令、结果与证据类型：
安全负向与恢复演练：
未运行项、真实阻塞、需要的环境/输入及解除条件：
提交/推送/合并/发布状态（分别写）：
父任务是否达到完成门槛，依据是什么：
下一批任务与依赖：
```

任务书的最终目标是用户可安装、可发现、可控制、可追溯地使用现有智能体和 Skill，并在个人闭环稳定后把同一能力扩展到真实局域网多设备。验收必须证明这些行为，而不仅是文件已落盘。

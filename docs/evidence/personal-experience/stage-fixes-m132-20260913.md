# M132 独立验收修复（2026-09-13）

本批按用户“按照你的建议修复，并提交”授权，在独立分支 `codex/m132-review-fixes-20260913` 完成修复。前置 M131/M132 快照单独记录为 `4aa15dd`，其后合同和修复分开提交。未合并 main、发布制品或修改用户服务。

## 结果与限制

- **R132-01 已收紧为安全拒绝**：生产 `fetchGitCLI` 在启动 Git、联网和创建克隆目标之前返回 `ErrGitTransportUnavailable`；导入 API 明确返回 503 `skill_import_git_transport_unavailable`，新版检查返回 503 `skill_update_source_unavailable`。没有运行配置可以打开旁路。HTTPS ZIP 继续走既有 DNS/IP 固定和重定向校验路径。
- 这意味着 **生产 Git 导入/新版检查暂不可用**，不是 Git 安全传输已经完成。保留本地 Git fixture 用于快照算法测试，不当作生产 Git 验收。后续必须完成实际地址固定、逐跳/替代对象来源校验和获取限额后才可恢复。Git 官方文档中 [http.curloptResolve 与 followRedirects](https://git-scm.com/docs/git-config/2.42.0.html) 是不同配置；仅限制 URL 协议不能证明完整传输边界。
- **R132-02 已修复**：新版检查有独立获取错误分类，不再把服务不可用或下载失败报告为安装变化；旧写流程的 `sourceError` 不变。URL 被拒、来源冲突、限额、取消和未支持来源分别处理；不返回原始上游诊断。
- **R132-03 已补齐**：规格 §3.12.35，update-check 请求/结果和 Git 导入请求三个 Schema、样例、Python 正负向校验；Go 检查真实结果与共享样例一致。运行时与前端继续验证 Schema 难以表达的总数/数组长度关系。
- **个人入口已接通**：已安装 Skill 页面提供原 HTTPS ZIP 链接输入、显式检查/取消、检查时间、内容差异与截断提示、导入候选和更新审阅入口。链接不落浏览器存储，输入改变清除旧结果；切换安装/离开页面取消请求；取消后的迟到响应不展示。错误或不匹配响应不能呈现“已是最新”。
- “只读”明确为不修改持久业务记录与授权，允许一次性暂存。新增整个状态目录的路径、文件内容摘要与权限比对，覆盖成功、失败、取消；获取层另验证失败/取消后的暂存清理。

## 验证

详细日志、浏览器截图与制品摘要见 [验证清单](fix-m132-20260913/verification.json)。

- Go 原有全模块测试通过，三相关包 race 通过，包含新增安全拒绝、错误映射、状态不变和取消清理测试；go vet、gofmt 通过。
- 前次验收两组负向测试已转为通过并纳入源码，不删除或弱化原断言。
- Python 合同 200 项通过；修改的 Python 合同测试 Ruff 通过。
- Web 22 文件 / 73 测试通过；企业和本地构建通过，更新 Go embed。
- 隔离 daemon + Playwright：7 次 fixture 请求覆盖变化、无差异、上游不可用、Git 未支持、错误安装编号、取消后迟到响应、390px 手机视图；没有页面异常或横向溢出。截图是明确的响应替身，不是真实 CDN 验收。
- CGO_ENABLED=0 四目标交叉构建：linux/amd64、linux/arm64、darwin/arm64、windows/amd64。构建不等同对应 OS 原生验收。

## 仍待完成

UX-010 保持 doing：安全 Git 传输、后台定期新版检查、通用旧状态写入拒绝与原生更新旅程继续待办；可信 Skill 执行来源、跨 OS/WorkBuddy 和 LAN 目标不在本批提升验收状态。

原 [独立验收报告](stage-acceptance-m132-20260913.md) 保留历史发现；其失败日志用于说明修复前行为，本记录说明修复后结果。原 M132 文档所写“210→200”实际为 201→200；此次不改历史样本，按真实测试记录。
